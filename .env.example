# API credentials (never commit real values)
ESTAT_APP_ID=replace-with-estat-app-id
CORP_APP_ID=
REALESTATE_API_KEY=

# Use separate random bearer tokens for each client.
MCP_AUTH_TOKEN=replace-with-general-token
MCP_AUTH_TOKEN_CODEX=replace-with-codex-token
MCP_AUTH_TOKEN_NOTION=replace-with-notion-token
